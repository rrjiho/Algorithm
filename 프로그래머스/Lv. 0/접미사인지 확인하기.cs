using System;

public class Solution {
    public int solution(string my_string, string is_suffix) {
        return my_string.EndsWith(is_suffix) ? 1 : 0;
    }
}
// System.string에 다양한 메서드를 이미 만들어 놓았기 때문에
// 알고 사용하는 것 또한 역량임을 알아야 함
