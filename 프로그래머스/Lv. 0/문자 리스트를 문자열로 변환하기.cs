using System;

public class Solution {
    public string solution(string[] arr) {
        
        string answer = "";
        
        foreach (string c in arr)
        {
            answer += c;
        }
        
        return answer;
    }
}
